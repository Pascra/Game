#pragma once

// Conversión entre píxeles y metros
#define PIXELS_TO_METERS(pixels) ((pixels) * 0.02f)  // Escala: 1 píxel = 0.02 metros
#define METERS_TO_PIXELS(meters) ((meters) * 50.0f)  // Escala inversa: 1 metro = 50 píxeles
#pragma once
